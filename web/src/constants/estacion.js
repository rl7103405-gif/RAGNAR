// Mapa usuario -> correo interno fijo de Firebase Auth (patron RUNA).
// El operador nunca ve ni escribe un correo real, solo el nombre de usuario.
export const ESTACION_EMAIL = {
  estacion: 'estacion@quini-ragnar.local'
}
